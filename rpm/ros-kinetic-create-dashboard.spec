Name:           ros-kinetic-create-dashboard
Version:        2.3.1
Release:        0%{?dist}
Summary:        ROS create_dashboard package

Group:          Development/Libraries
License:        BSD
URL:            http://ros.org/wiki/create_dashboard
Source0:        %{name}-%{version}.tar.gz

Requires:       ros-kinetic-create-node
Requires:       ros-kinetic-diagnostic-msgs
Requires:       ros-kinetic-rospy
Requires:       ros-kinetic-rqt-gui
Requires:       ros-kinetic-rqt-gui-py
Requires:       ros-kinetic-rqt-robot-dashboard
BuildRequires:  ros-kinetic-catkin
BuildRequires:  ros-kinetic-create-node
BuildRequires:  ros-kinetic-diagnostic-msgs
BuildRequires:  ros-kinetic-rospy
BuildRequires:  ros-kinetic-rqt-gui
BuildRequires:  ros-kinetic-rqt-gui-py
BuildRequires:  ros-kinetic-rqt-robot-dashboard

%description
The Create dashboard is a RQT-based plug-in for visualising data from the Create
and giving easy access to basic functionalities.

%prep
%setup -q

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/kinetic/setup.sh" ]; then . "/opt/ros/kinetic/setup.sh"; fi
mkdir -p obj-%{_target_platform} && cd obj-%{_target_platform}
%cmake .. \
        -UINCLUDE_INSTALL_DIR \
        -ULIB_INSTALL_DIR \
        -USYSCONF_INSTALL_DIR \
        -USHARE_INSTALL_PREFIX \
        -ULIB_SUFFIX \
        -DCMAKE_INSTALL_LIBDIR="lib" \
        -DCMAKE_INSTALL_PREFIX="/opt/ros/kinetic" \
        -DCMAKE_PREFIX_PATH="/opt/ros/kinetic" \
        -DSETUPTOOLS_DEB_LAYOUT=OFF \
        -DCATKIN_BUILD_BINARY_PACKAGE="1" \

make %{?_smp_mflags}

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/kinetic/setup.sh" ]; then . "/opt/ros/kinetic/setup.sh"; fi
cd obj-%{_target_platform}
make %{?_smp_mflags} install DESTDIR=%{buildroot}

%files
/opt/ros/kinetic

%changelog
* Thu Jun 30 2016 Marcus Liebhardt <marcus.liebhardt@yujinrobot.com> - 2.3.1-0
- Autogenerated by Bloom

